spring:
  application:
    name: github-action-practice-test
  datasource:
    url: jdbc:h2:mem:gitdb
    driver-class-name: org.h2.Driver
    username: gittestdb
    password: gittestdb123
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    show-sql: true
    defer-datasource-initialization: false